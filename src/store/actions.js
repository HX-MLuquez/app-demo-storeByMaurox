export function addFavorites(ch) {
  console.log("in addFavorites is ch ---> ", ch);
  return ch;
}

export function deleteFavorites(id) {
  return id;
}

export function filterCards(status) {
  return status;
}
export function orderCards(order) {
  return order;
}

export function reset() {
  return;
}
